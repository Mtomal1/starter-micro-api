<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link rel="stylesheet" href="/css/main.css" />

</head>

<body tabindex="-1">
    <%- include('partials/navbar' ,{page:"/lego/sets"}) %>

        <br />

        <div class="container mx-auto">


            <div class="hero bg-base-200 rounded-lg p-6">
                <div class="hero-content text-center">
                    <div class="max-w-md">
                        <h1 class="text-5xl font-bold">Collection</h1>

                        <br>

                        <p class="py-6">Browse the entire collection. Click the theme to filter by themes such as:
                        <!-- <div class="flex justify-center mt-4"> -->
                            <a href="/lego/sets?theme=technic" class="btn btn-primary mx-2">Technic</a>
                            <a href="/lego/sets?theme=classic town" class="btn btn-primary mx-2">Classic Town</a>
                            <a href="/lego/sets?theme=supplemental" class="btn btn-primary mx-2">Supplemental</a>
                        <!-- </div> -->
                        </p>
                    </div>
                </div>
            </div>

            <table class="table table-compact mt-4">
                <tbody>
                    <% for (const user of sets){ %>
                        <!-- <% sets.forEach(set=> { %> -->
                        <tr>
                            <td>
                                <div class="avatar">
                                    <div class="w-32 h-32 rounded-lg bg-transparent bg-no-repeat bg-cover">
                                        <img src="<%= set.img_url %>" width="400">
                                    </div>
                                </div>
                            </td>

                            <td>
                                <%= set.name %>
                                    <span>|</span>
                                    <a href="/lego/sets?theme=<%= set.theme %>" class="btn  mt-2 btn-xs">
                                        <%= set.theme %>
                                    </a>
                            </td>
                            <td>
                                <span class="font-bold">Year: </span>
                                <%= set.year %>

                                    <span class="font-bold">Parts:</span>
                                    <%= set.num_parts %>
                            </td>
                            <td><a href="/lego/sets/<%= set.set_num %>" class="btn btn-xs">Details</a></td>

                        </tr>
                        <!-- <% }); %> -->
                        <% }; %>
                </tbody>
            </table>



        </div>
</body>

</html>